import { HiddenTextDirective } from './hidden-text.directive';

describe('HiddenTextDirective', () => {
  it('should create an instance', () => {
    const directive = new HiddenTextDirective();
    expect(directive).toBeTruthy();
  });
});
