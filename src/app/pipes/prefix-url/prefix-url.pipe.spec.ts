import { PrefixUrlPipe } from './prefix-url.pipe'

describe('PrefixUrlPipe', () => {
    it('create an instance', () => {
        const pipe = new PrefixUrlPipe()
        expect(pipe).toBeTruthy()
    })
})
